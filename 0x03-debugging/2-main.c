#include <stdio.h>
#include "main.h"

/**
* main - prints the largest of 3 integers
* Return: 0
*/

int main(void)
{
	int largest;

	largest = largest_number(972, -98, 0);

       printf("%d is the largest number\n", largest);

        return (0);
}
