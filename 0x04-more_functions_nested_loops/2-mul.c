#include "main.h"
#include <stdio.h>
/**
 * mul(int a, int b) - multiplies two integers.
 * int a, int b: are the operand
 * Return: Always 0 if successful
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	
	return (c);
}
